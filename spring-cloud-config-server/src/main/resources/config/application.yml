﻿server.port: 8888
spring: 
  cloud:
    config:
      server:
        git:
          uri: https://github.com/bj/spring-cloud-config-center
          searchPaths: '{application}'
  application.name: spring-cloud-config-server